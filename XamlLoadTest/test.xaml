<Window xmlns="root" xmlns:x="special">
    <Collection>
        <Button x:Name="MyButton" Content="CONTENIDO"></Button>
        <Button Content="{Reference Target=MyButton.Content}"></Button>
    </Collection>
</Window>